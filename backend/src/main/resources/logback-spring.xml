<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- ===== ローカル開発プロファイル: カラー付きテキスト ===== -->
  <springProfile name="default,local">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
        <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) %cyan([%thread]) %yellow(%logger{36}) - %msg%n</pattern>
        <charset>UTF-8</charset>
      </encoder>
    </appender>

    <logger name="online.diaperlicense" level="DEBUG"/>
    <logger name="org.springframework.security" level="DEBUG"/>
    <logger name="org.mybatis" level="DEBUG"/>
    <logger name="org.flywaydb" level="INFO"/>

    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

  <!-- ===== 本番プロファイル: JSON 構造化ログ ===== -->
  <springProfile name="prod">
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeContext>false</includeContext>
        <fieldNames>
          <timestamp>timestamp</timestamp>
          <version>[ignore]</version>
          <levelValue>[ignore]</levelValue>
        </fieldNames>
      </encoder>
    </appender>

    <logger name="online.diaperlicense" level="INFO"/>
    <logger name="org.springframework.security" level="WARN"/>
    <logger name="org.mybatis" level="WARN"/>
    <logger name="org.flywaydb" level="INFO"/>

    <root level="WARN">
      <appender-ref ref="CONSOLE_JSON"/>
    </root>
  </springProfile>

</configuration>
